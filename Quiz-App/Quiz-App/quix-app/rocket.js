// You can add any additional rocket-related JavaScript functionality here if needed
// Currently, the rocket animation is handled entirely through CSS
document.addEventListener('DOMContentLoaded', () => {
    const rocket = document.querySelector('.rocket');
    
    // Optional: You could add interactive rocket behaviors here
    rocket.addEventListener('click', () => {
        console.log('Rocket clicked!');
        // Potential future interactions
    });
});